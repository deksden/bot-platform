# Отчет: MBB / documentation routing

## Compliance findings
- Протокол в целом соблюдает SSoT-разделение: framework-contract остается в `bot-platform`, а product truth явно удерживается в `docoved-agent` и `seller-agent` (`/Users/deksden/Documents/_Projects/bot-platform/.memory-bank/plans/protocols/PRT-042-channel-runtime-canonical-document-command-and-rendering.md:52`, `:57`, `:58`, `:59`).
- Нарушение frontmatter-стандарта по тегам: в протоколе 8 тегов, тогда как MBB рекомендует 3-6 релевантных тегов (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:9`, `/Users/deksden/Documents/_Projects/bot-platform/.memory-bank/mbb/frontmatter-standards.md:145`).
- В `related_files` используются абсолютные cross-repo пути; это хуже согласуется с MBB-правилом про относительные пути и усложняет переносимость ссылок (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:19`, `:20`, `:21`, `:22`, `/Users/deksden/Documents/_Projects/bot-platform/.memory-bank/mbb/frontmatter-standards.md:50`).
- Общая идея protocol-слоя корректная: он оформлен как curated execution document, а не как дубль feature/spec, и это совпадает с MBB delivery-docs guide (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:255`, `/Users/deksden/Documents/_Projects/bot-platform/.memory-bank/mbb/delivery-docs-guide.md:119`, `:121`, `:143`).

## Missing docs/index updates
- В `bot-platform` не хватает явного runtime-entry для нового `channel-runtime` контракта: `spec/runtime/index.md` пока перечисляет только существующие runtime docs и не резервирует слот под `channel-runtime-contract.md` (`/Users/deksden/Documents/_Projects/bot-platform/.memory-bank/spec/runtime/index.md:64`, `:66`, `:67`, `:68`, `:69`, `:70`, `:71`, `:72`, `:73`, `:74`).
- В `bot-platform/spec/index.md` тоже нет ссылки на будущий `spec/runtime/channel-runtime-contract.md` ни в current spec sections, ни в must-exist inventory (`/Users/deksden/Documents/_Projects/bot-platform/.memory-bank/spec/index.md:119`, `:120`).
- В `docoved-agent` upstream cleanup routing пока останавливается на `PRT-041`; для этой волны нужен явный мост к новому shared channel-runtime контракту, иначе docoved adapter-proof не увидит, куда идти за каноном (`/Users/deksden/Documents/_Projects/docoved-agent/.memory-bank/plans/index.md:137`, `:138`, `:139`, `:146`, `:148`).
- В `seller-agent` routing аналогично остается на `PRT-041` и `PRT-038`; для channel-runtime adoption не хватает явного upstream-указателя на `PRT-042` (`/Users/deksden/Documents/_Projects/seller-agent/.memory-bank/plans/index.md:86`, `:87`, `:89`, `:92`, `:93`).
- В `sales-agent` уже есть split-routing к target repos, но этот индекс стоит держать как mixed-tracker only; каноническая ownership-ссылка должна жить в `seller-agent`, а не в tracker repo (`/Users/deksden/Documents/_Projects/sales-agent/.memory-bank/index.md:32`, `:35`, `:36`, `:37`).

## Cross-repo routing issues
- Формулировка `SellerAgent/sales-agent` в closure criteria смешивает canonical owner и historical tracker; это лучше разделить на два отдельных адресата: `seller-agent` как product owner и `sales-agent` как mixed tracker (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:385`).
- Раздел `Sales-agent mixed repo` правильно запрещает `sales-agent` быть canonical owner, но дальше всё ещё требует, чтобы Memory Bank links “point to” `seller-agent`; это нужно сделать явно и без смешанного ярлыка `SellerAgent/sales-agent` (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:246`, `:248`, `:250`, `:251`, `:252`, `:253`).
- Для Docoved и SellerAgent не хватает симметричного routing к `PRT-042`, хотя протокол прямо описывает отдельные adoption phases для обоих продуктов (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:287`, `:301`, `:313`).
- В `sales-agent` уже есть canonical-link routing к `seller-agent`; дополнительный canonical link туда не нужен, но текст должен подчеркнуть, что это именно target repo, а не продолжение mixed ownership (`/Users/deksden/Documents/_Projects/sales-agent/.memory-bank/index.md:32`, `:36`).

## Exact protocol edits
- Сократить `tags` до 3-6 релевантных значений; безопасный вариант: `protocol, channel-runtime, commands, rendering, contracts, docoved` или `... , selleragent` вместо текущих 8 тегов (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:9`).
- Добавить в `related_files` как минимум `/.memory-bank/spec/runtime/command-framework-contract.md` и, после создания, `/.memory-bank/spec/runtime/channel-runtime-contract.md`, чтобы contract graph не висел только на `runtime/index.md` (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:18`).
- Заменить блок `Sales-agent mixed repo` на явную маршрутизацию: `bot-platform` = framework contract, `docoved-agent` = Docoved adoption, `seller-agent` = SellerAgent adoption, `sales-agent` = historical/mixed tracker only (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:246`, `:248`, `:250`, `:251`, `:252`, `:253`).
- В `Phase 2: Framework docs` явно перечислить, что `spec/runtime/index.md` и `spec/index.md` должны быть обновлены вместе с новым `spec/runtime/channel-runtime-contract.md` (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:273`, `:275`, `:276`, `:277`, `:278`).
- В `Phase 3` и `Phase 5` добавить формулировку, что соответствующие Memory Bank root/plans indexes должны быть обновлены в той же волне, иначе adoption proof будет без маршрута к канону (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:287`, `:313`).
- В closure criteria заменить `SellerAgent/sales-agent` на отдельные сущности: `seller-agent` must route as canonical owner; `sales-agent` must remain tracker/lineage only (`...PRT-042-channel-runtime-canonical-document-command-and-rendering.md:384`, `:385`).
